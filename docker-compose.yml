networks:
  default:
    external: true
    name: bridge

services:
  app:
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
      args:
        # ============================================
        # BUILD-TIME VARIABLES (NEXT_PUBLIC_*)
        # ============================================
        # These are embedded into the JavaScript bundles during build.
        # They are "baked" into the image and cannot be changed without rebuilding.
        #
        # How it works:
        # 1. Define in your .env file (root directory) or shell: NEXT_PUBLIC_BETTER_AUTH_BASEURL=...
        # 2. Docker Compose reads ${VARIABLE} and passes as ARG to Dockerfile
        # 3. Dockerfile converts ARG to ENV for Next.js build process
        # 4. Next.js hardcodes these values into static JS files
        - NEXT_PUBLIC_BETTER_AUTH_BASEURL=${NEXT_PUBLIC_BETTER_AUTH_BASEURL:-}
        - NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=${NEXT_PUBLIC_GOOGLE_MAPS_API_KEY:-}
    container_name: hr-automation
    ports:
      - "3001:3000"

    # ============================================
    # RUNTIME VARIABLES (Server-side & Secrets)
    # ============================================
    # These are injected into the container at startup and can be changed
    # without rebuilding the image.
    #
    # The env_file directive loads ALL variables from ./apps/web/.env
    # into the container. No need to list them individually.
    #
    # Create ./apps/web/.env with your variables:
    # DATABASE_URL=postgresql://...
    # BETTER_AUTH_SECRET=your-secret
    # GOOGLE_CLIENT_ID=...
    # etc.
    env_file:
      - ./apps/web/.env
    environment:
      # Core runtime configuration (always set)
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
      # All other variables (DATABASE_URL, BETTER_AUTH_SECRET, etc.)
      # are loaded from ./apps/web/.env via env_file above
    restart: unless-stopped
    # DNS configuration (optional - uncomment if you have DNS issues)
    # dns:
    #   - 8.8.8.8
    #   - 8.8.4.4
    # Health check to ensure the application is running
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "require('http').get('http://localhost:3000/api/health', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
