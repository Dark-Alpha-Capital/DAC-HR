// Client-side stub for the `cloudflare:workers` module (see `environmentAlias`
// in vite.config.ts). On the client there are no Worker bindings, so every
// binding is absent.
export const env = {} satisfies Partial<Cloudflare.Env>;
