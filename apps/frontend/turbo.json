{
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_DEFAULT$", ".env*", ".dev.vars", ".dev.vars*"]
    },
    "test": {
      "inputs": ["$TURBO_DEFAULT$", ".env*", ".dev.vars", ".dev.vars*"]
    }
  }
}
